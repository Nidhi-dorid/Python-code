#Palindrome number are the numbers that are same as we read backward or forward.

num=int(input("Enter number:"))
rev=0
float=num
while(num>0):
    x=num%10
    rev=rev*10+dig
    num=num//10
if(float==rev):
    print("The number is a palindrome!")
else:
    print("error")
