import numpy as np
from numpy import*
m=int(input("enter number id rows"))
n=int(input("enter number id columns"))
a=zeros((m,n),dtype=int)

p=len(a)

i=0
j=0
for i in range(p):
  for j in range(len(a[i])):
    x=int(input("enter the numbers: "))
    a[i][j]=x
print(a)
